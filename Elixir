local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Elixir-Ogii hub",
    Icon = "", 
    Author = "by .ftgs and .ftgs",
    
    OpenButton = {
        Title = "Open Ogii's Script",
        Icon = "monitor",
        CornerRadius = UDim.new(0,16),
        StrokeThickness = 2,
        Color = ColorSequence.new(
            Color3.fromHex("FF0F7B"), 
            Color3.fromHex("F89B29")
        ),
        OnlyMobile = true, 
        Enabled = true,
        Draggable = true,
    }
})

-- WELCOME NOTIFICATION
WindUI:Notify({
    Title = "Elixir-Ogii hub",
    Content = "All Systems Active - Mobile Optimized",
    Duration = 5
})

-- TABS
local MainTab = Window:Tab({ Title = "Main", Icon = "bird", Locked = false })
local TechTab = Window:Tab({ Title = "Tech", Icon = "cpu", Locked = false })
local HubTab = Window:Tab({ Title = "Hub section", Icon = "layout-grid", Locked = false })
local AccessoryTab = Window:Tab({ Title = "Accessory", Icon = "package", Locked = false })
local LagTab = Window:Tab({ Title = "Lag Fixes", Icon = "zap", Locked = false })
local SettingsTab = Window:Tab({ Title = "Settings", Icon = "settings", Locked = false })

--- MAIN SECTION ---

MainTab:Section({ Title = "Combat & Movement" })

-- Fly GUI Button
MainTab:Button({
    Title = "Open Fly GUI (v3)",
    Detail = "Loads a separate flying menu",
    Callback = function()
        pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Gui-Fly-v3-37111"))() end)
    end
})

-- WalkSpeed with Mobile Fix Loop
local SpeedValue = 16
MainTab:Slider({
    Title = "WalkSpeed",
    Detail = "Slide to change speed (Fixed for Mobile)",
    Step = 1, Min = 16, Max = 250, Default = 16,
    Callback = function(v) SpeedValue = v end
})

-- Permanent Speed Loop (Beats TSB anti-speed)
task.spawn(function()
    while true do
        pcall(function()
            local char = game.Players.LocalPlayer.Character
            if char and char:FindFirstChild("Humanoid") then
                char.Humanoid.WalkSpeed = SpeedValue
            end
        end)
        task.wait(0.1)
    end
end)

-- Smooth Aimlock
local AimlockEnabled = false
MainTab:Toggle({
    Title = "Smooth Aimlock",
    Value = false,
    Callback = function(state)
        AimlockEnabled = state
        task.spawn(function()
            local Camera = workspace.CurrentCamera
            while AimlockEnabled do
                local closestPlayer = nil
                local shortestDistance = math.huge
                for _, v in pairs(game.Players:GetPlayers()) do
                    if v ~= game.Players.LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                        local pos, onScreen = Camera:WorldToViewportPoint(v.Character.PrimaryPart.Position)
                        if onScreen then
                            local magnitude = (Vector2.new(pos.X, pos.Y) - Vector2.new(Camera.ViewportSize.X/2, Camera.ViewportSize.Y/2)).magnitude
                            if magnitude < shortestDistance then
                                closestPlayer = v
                                shortestDistance = magnitude
                            end
                        end
                    end
                end
                if closestPlayer then
                    Camera.CFrame = CFrame.new(Camera.CFrame.Position, closestPlayer.Character.HumanoidRootPart.Position)
                end
                task.wait()
            end
        end)
    end
})

MainTab:Toggle({
    Title = "Auto Block",
    Value = false,
    Callback = function(state)
        _G.AutoBlock = state
        task.spawn(function()
            local VIM = game:GetService("VirtualInputManager")
            while _G.AutoBlock do
                pcall(function()
                    local char = game.Players.LocalPlayer.Character
                    if char and char:FindFirstChild("HumanoidRootPart") then
                        local shouldBlock = false
                        for _, v in pairs(game.Players:GetPlayers()) do
                            if v ~= game.Players.LocalPlayer and v.Character and v.Character:FindFirstChild("HumanoidRootPart") then
                                if (char.HumanoidRootPart.Position - v.Character.HumanoidRootPart.Position).Magnitude < 18 then
                                    shouldBlock = true break
                                end
                            end
                        end
                        VIM:SendKeyEvent(shouldBlock, Enum.KeyCode.F, false, game)
                    end
                end)
                task.wait(0.1)
            end
        end)
    end
})

MainTab:Section({ Title = "Visuals" })

MainTab:Toggle({
    Title = "Fullbright & No Shadow",
    Value = false,
    Callback = function(state)
        _G.FullBright = state
        task.spawn(function()
            while _G.FullBright do
                game.Lighting.Brightness = 2
                game.Lighting.GlobalShadows = false
                game.Lighting.ClockTime = 14
                task.wait(0.5)
            end
        end)
    end
})

--- LAG FIXES ---

LagTab:Section({ Title = "Visual Enhancement" })

LagTab:Button({
    Title = "Full Graphics Upgrade",
    Callback = function()
        pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-FULL-GRAPHIC-UPGRADE-4582"))() end)
    end
})

LagTab:Section({ Title = "Optimization" })

LagTab:Button({
    Title = "Remove Textures",
    Callback = function()
        pcall(function()
            for _, v in pairs(game:GetDescendants()) do
                if v:IsA("Texture") or v:IsA("Decal") then v:Destroy() end
            end
        end)
    end
})

--- TECH & HUB (ORIGINAL SCRIPTS) ---

TechTab:Toggle({ Title = "Instant twisted", Callback = function(s) if s then pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/yqantg-pixel/Find/refs/heads/main/Protected_3334988263341522.lua.txt"))() end) end end })
HubTab:Button({ Title = "MinhNhat", Callback = function() pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-MinhNhat-Tsb-62161"))() end) end })
AccessoryTab:Button({ Title = "Headless and Korblox", Callback = function() pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/The-Strongest-Battlegrounds-tsb-client-sided-headless-and-korblox-74593"))() end) end })

--- SETTINGS SECTION ---

SettingsTab:Section({ Title = "Server & Utility" })

local antiAfkConnection
SettingsTab:Toggle({
    Title = "Anti-AFK",
    Value = false,
    Callback = function(state)
        if state then
            antiAfkConnection = game.Players.LocalPlayer.Idled:Connect(function()
                game:GetService("VirtualUser"):CaptureController()
                game:GetService("VirtualUser"):ClickButton2(Vector2.new())
            end)
        else
            if antiAfkConnection then antiAfkConnection:Disconnect() end
        end
    end
})

SettingsTab:Button({
    Title = "Server Hop",
    Callback = function()
        local Servers = game:GetService("HttpService"):JSONDecode(game:HttpGet("https://games.roblox.com/v1/games/"..game.PlaceId.."/servers/Public?sortOrder=Asc&limit=100"))
        for _, v in pairs(Servers.data) do
            if v.playing < v.maxPlayers and v.id ~= game.JobId then
                game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, v.id)
                break
            end
        end
    end
})

SettingsTab:Button({ Title = "Rejoin Server", Callback = function() game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, game.JobId, game.Players.LocalPlayer) end })
SettingsTab:Button({ Title = "Destroy UI", Callback = function() Window:Destroy() end })

MainTab:Select()
