local WindUI = loadstring(game:HttpGet("https://github.com/Footagesus/WindUI/releases/latest/download/main.lua"))()

local Window = WindUI:CreateWindow({
    Title = "Elixir-Ogii hub",
    Icon = "", 
    Author = "by .ftgs and .ftgs",
    
    OpenButton = {
        Title = "Open Ogii's Script",
        Icon = "monitor",
        CornerRadius = UDim.new(0,16),
        StrokeThickness = 2,
        Color = ColorSequence.new(Color3.fromHex("FF0F7B"), Color3.fromHex("F89B29")),
        OnlyMobile = true, 
        Enabled = true,
        Draggable = true,
    }
})

-- Welcome Notification
WindUI:Notify({
    Title = "Elixir-Ogii hub",
    Content = "Welcome To My Script",
    Duration = 5
})

-- TABS
local MainTab = Window:Tab({ Title = "Main", Icon = "bird", Locked = false })
local TechTab = Window:Tab({ Title = "Tech", Icon = "cpu", Locked = false })
local HubTab = Window:Tab({ Title = "Hub section", Icon = "layout-grid", Locked = false })
local BloxFruitTab = Window:Tab({ Title = "Blox Fruit: Bounty Hunt", Icon = "sword", Locked = false })
local AccessoryTab = Window:Tab({ Title = "Accessory", Icon = "package", Locked = false })
local LagTab = Window:Tab({ Title = "Lag Fixes", Icon = "zap", Locked = false })
local SettingsTab = Window:Tab({ Title = "Settings", Icon = "settings", Locked = false })

--- MAIN SECTION ---
MainTab:Section({ Title = "Combat & Movement" })

MainTab:Button({
    Title = "Aimlock",
    Detail = "Silent Aim & LockOn",
    Callback = function() 
        pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/The-Strongest-Battlegrounds-Tsb-Op-Silent-And-LockOn-44541"))() end) 
    end
})

MainTab:Button({
    Title = "Open Fly GUI (v3)",
    Callback = function() 
        pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-Gui-Fly-v3-37111"))() end) 
    end
})

--- TECH SECTION ---
TechTab:Section({ Title = "Tech Scripts" })

TechTab:Toggle({
    Title = "Instant twisted",
    Callback = function(state)
        if state then
            local parts = {"aHR0cHM6Ly9yYXcuZ2l0aHVidXNlcmNvbnRlbnQuY29tL2xldHVh","bmtpZXQwOTgxMjM0NS1jbXlrL1Rlc3RraWJhL3JlZnMvaGVhZHMv","bWFpbi9lZGZhMzg1ZWY1YzQ3ZDNhLmx1YQ=="}
            local b='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/'
            local function d(data)
                data = data:gsub('[^'..b..'=]', '')
                return (data:gsub('.', function(x)
                    if x == '=' then return '' end
                    local r, f = '', (b:find(x) - 1)
                    for i = 6, 1, -1 do r = r .. (f % 2^i - f % 2^(i-1) > 0 and '1' or '0') end
                    return r
                end):gsub('%d%d%d?%d?%d?%d?%d?%d?', function(x)
                    if #x ~= 8 then return '' end
                    local c = 0
                    for i = 1, 8 do c = c + (x:sub(i, i) == '1' and 2^(8-i) or 0) end
                    return string.char(c)
                end))
            end
            pcall(function() loadstring(game:HttpGet(d(table.concat(parts))))() end)
        end
    end
})

TechTab:Toggle({ Title = "Instant lethal", Callback = function(s) if s then pcall(function() loadstring(game:HttpGet("https://api.jnkie.com/api/v1/luascripts/public/57a4d240a2440f0450986c966469092ccfb8d4797392cb8f469fa8b6e605e64d/download"))() end) end end })
TechTab:Toggle({ Title = "Oreo", Callback = function(s) if s then pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/dinhthanhtuankiet1762009-sudo/Js/refs/heads/main/dd8ef3753bbaf2b2.lua"))() end) end end })
TechTab:Toggle({ Title = "kiba", Callback = function(s) if s then pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/yqantg-pixel/Find/refs/heads/main/Protected_3334988263341522.lua.txt"))() end) end end })

--- HUB SECTION ---
HubTab:Section({ Title = "Other Hubs" })
HubTab:Button({ Title = "Lunar", Callback = function() pcall(function() loadstring(game:HttpGet("https://pastefy.app/ADhVPN6M/raw"))() end) end })
HubTab:Button({ Title = "MinhNhat", Callback = function() pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-MinhNhat-Tsb-62161"))() end) end })

--- BLOX FRUIT SECTION ---
BloxFruitTab:Section({ Title = "PVP & Bounty" })

BloxFruitTab:Button({
    Title = "Auto bounty hunt",
    Detail = "Hermanos Hub (PVP Mode)",
    Callback = function()
        pcall(function()
            getgenv().script_mode = "PVP"
            loadstring(game:HttpGet("https://raw.githubusercontent.com/hermanos-dev/hermanos-hub/refs/heads/main/Loader.lua"))()
        end)
    end
})

--- ACCESSORY SECTION ---
AccessoryTab:Section({ Title = "Visuals" })
AccessoryTab:Button({ Title = "Headless and Korblox", Callback = function() pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/The-Strongest-Battlegrounds-tsb-client-sided-headless-and-korblox-74593"))() end) end })
AccessoryTab:Button({ Title = "Erisyphia staff", Callback = function() pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/GuestlyTheGreatestGuest/Scripts/refs/heads/main/Erisyphia-Staff-made-by-Guestly"))() end) end })
AccessoryTab:Button({ Title = "Friery Horns", Callback = function() pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/GuestlyTheGreatestGuest/Scripts/refs/heads/main/Fiery-Horns-of-the-Netherworld-made-by-Guestly"))() end) end })

--- LAG FIXES ---
LagTab:Section({ Title = "Optimization" })
LagTab:Button({ Title = "Bloxstrap Initiate", Callback = function() pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/qwertyui-is-back/Bloxstrap/main/Initiate.lua"))() end) end })
LagTab:Button({ Title = "Full Graphics Upgrade", Callback = function() pcall(function() loadstring(game:HttpGet("https://rawscripts.net/raw/Universal-Script-FULL-GRAPHIC-UPGRADE-4582"))() end) end })

--- SETTINGS ---
SettingsTab:Section({ Title = "System" })
SettingsTab:Toggle({ Title = "Anti-AFK", Callback = function(state) if state then _G.AFK = game.Players.LocalPlayer.Idled:Connect(function() game:GetService("VirtualUser"):Button2Down(Vector2.new(0,0),workspace.CurrentCamera.CFrame) task.wait(1) game:GetService("VirtualUser"):Button2Up(Vector2.new(0,0),workspace.CurrentCamera.CFrame) end) else if _G.AFK then _G.AFK:Disconnect() end end end })
SettingsTab:Button({ Title = "Server Hop", Callback = function() pcall(function() loadstring(game:HttpGet("https://raw.githubusercontent.com/78n/rejoin/main/hop.lua"))() end) end })
SettingsTab:Button({ Title = "Rejoin", Callback = function() game:GetService("TeleportService"):TeleportToPlaceInstance(game.PlaceId, game.JobId) end })

MainTab:Select()
